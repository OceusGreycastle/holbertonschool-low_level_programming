#include <stdio.h>
#include <time.h>
/**
* main - prints a specific line
* Return: 0
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf("%d \n", x);
	if (x > 0)
	{
		printf("is positive \n");
	}
	else if (x < 0)
	{
		printf("is negative \n");
	}
	else
	}
		printf("is zero \n");
	}
	return (0);
}
