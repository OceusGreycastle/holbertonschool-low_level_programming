#include "main.h"

/**
*is_prime_number - reads for prime numbers
*@n: Variable for Prime Integer
*Return: 0
*/

int is_prime_number(int n)
{
	if (n / n || n / 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
